pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                script {
                    // Executar Docker Compose
                    sh 'docker-compose up'
                }
            }
        }
    }

    post {
        always {
            // Limpar recursos
            cleanWs()
        }

        success {
            echo 'Pipeline succeeded!'
        }

        failure {
            echo 'Pipeline failed!'
        }
    }
}
