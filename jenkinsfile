version: '3.9'

pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                script {
                    // Executar Docker Compose no host da máquina
                    sh 'sudo chmod +x entrypoint.sh'
                    sh 'sudo sudo ufw allow 44392'
                    sh 'docker compose up'
                }
            }
        }
    }

    post {
        always {
            // Limpar recursos
            cleanWs()
        }

        success {
            echo 'Pipeline succeeded!'
        }

        failure {
            echo 'Pipeline failed!'
        }
    }
}
