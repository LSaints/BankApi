version: '3.8'

pipeline {
    agent any

    stages {

        stage('Deploy') {
            steps {
                script {
                    sh 'echo :: Realizando build com docker compose ::'
                    sh 'sudo docker compose up --build'
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
